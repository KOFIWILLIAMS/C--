#include <iostream>

using namespace std;
int pm;
int new_pm;
int g;
string male;
int main()
{

    cout << "the pocket money: ";
    cin>> pm;
    cout << "the new pocket money of a student ";
    cin>> new_pm;
    cout << "the gender of a student ";
    cin>> g;
    
    if (g = male)
	{
	new_pm = (pm * 0.1) + pm
	};
	else()
    new_pm = (pm * 0.2) + pm;
    cout << "display the result " << new_pm;
    return 0;
}
